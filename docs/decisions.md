# 意思決定ログ（ADRライト）

設計・方針の決定を時系列で記録します。後から覆す場合は、古いエントリを消さず
新しいエントリで「これにより ADR-00X を更新」と書いて履歴を残します。

書式: `ADR-番号 | 日付 | 決定 | 理由 | 影響範囲 | 状態(有効/更新済)`

---

## ADR-001 | 2026-05-30 | クレジットカードは発生主義で計上
- **決定**: クレカ利用は「買った日（accrual_date）」にPL費用計上。現金の引き落としは別管理。
- **理由**: 正確なPL（損益発生のタイミング）を出すため。「肌感覚」の維持とも相性が良い。
- **影響範囲**: transactions, card_statements, transfers の設計全体。
- **状態**: 有効

## ADR-002 | 2026-05-30 | 残高はカラムで持たず取引から算出する
- **決定**: wallets に balance カラムを持たせず、initial_balance + 取引の集計で算出。
- **理由**: データ不整合を防ぐ（Single Source of Truth）。修正・削除に強い。
- **影響範囲**: wallets, 残高表示ロジック全般。
- **状態**: 有効

## ADR-003 | 2026-05-30 | クレカ引き落としは transfers で消込する
- **決定**: 支払日の引き落としは「銀行→カード」の transfers 1件で表現し、statement を paid にする。
- **理由**: 振替機能とクレカ消込を同一の仕組みで扱え、設計がシンプルになる。
- **影響範囲**: transfers, card_statements。
- **状態**: 有効

## ADR-004 | 2026-05-30 | 将来のマルチユーザー対応を前提に設計
- **決定**: 全テーブルに user_id を持たせる。
- **理由**: 現在は個人利用だが、将来家族等への拡張余地を残すため。
- **影響範囲**: 全テーブル。
- **状態**: 有効

## ADR-005 | 2026-05-30 | スタックは Next.js + PostgreSQL、ローカル先行
- **決定**: フロントは Next.js(PWA志向)、DBはPostgreSQL。まずローカルで設計・実装し、後でAWS。
- **理由**: 学習目的（DB設計を手で動かす）と手戻り最小化。
- **影響範囲**: 全体。
- **状態**: 有効

## ADR-006 | 2026-05-30 | UI/UX確認フェーズは単一HTMLのモックアップで行う
- **決定**: Next.js本体を立てる前に、捨てやすい mockup/index.html で画面と操作感を確認。
- **理由**: 要件更新前に本体を作り込むと手戻りが大きいため。
- **影響範囲**: Phase 1 の進め方。
- **状態**: 有効

## ADR-007 | 2026-05-31 | 会計年度（FY）を4月〜翌3月とする
- **決定**: 集計・振り返りの年度境界を4月〜翌3月に置く。
- **理由**: ユーザーがFYの考え方で年間PLを振り返る運用をしているため。
- **影響範囲**: 月次・年次の集計クエリ、ダッシュボードの期間選択。
- **状態**: 🔁 更新済（ADR-017 によりFY開始月を可変設定に変更）

## ADR-008 | 2026-05-31 | 変動費カテゴリは多階層・取引は最下層(葉)に入力
- **決定**: categories を3〜4階層の自己参照ツリーとし、取引(transactions)は葉カテゴリにのみ紐づける。上位（食費(1人)・食費・変動費など）は集計で算出。
- **理由**: 1円単位で支出ボトルネックを点検したいという運用。集計ノードに直接入力させると二重計上の恐れ。
- **影響範囲**: categories, transactions, ダッシュボード集計。
- **状態**: 有効

## ADR-009 | 2026-05-31 | 給与明細は金額とKPI(労働時間等)を分けて保持
- **決定**: payslip に金額項目と時間項目を保持し、時給換算等は算出で出す。労働時間KPIは管理対象とする。
- **理由**: 「働き方の効率」を振り返る指標として価値があるとユーザーが判断（Q2）。
- **影響範囲**: payslips 周辺の設計。
- **状態**: 有効（Q2 確定）

## ADR-010 | 2026-05-31 | 経費精算・借入金は pl_type='excluded'（PL対象外）で扱う
- **決定**: PL区分に `excluded` を追加。経費立替・経費精算・借入金・返済等は、ウォレット残高には反映するがPL（収入/費用/最終利益・各種比率）の集計からは除外する。記録自体は両建てで残す。
- **理由**: 立替経費は本来ユーザーの費用ではなく「立替金（資産）」、借入金は「負債」。両建てで収入/費用に載せると総額が水増しされ、変動費率・貯蓄率が歪む。一方で記録を残すことで"片方だけ入力"の漏れは防げる（ユーザーの当初目的を達成）。
- **影響範囲**: categories.pl_type の値、ダッシュボードのPL集計ロジック。
- **状態**: 有効（Q1 確定）。これにより ADR-001 系のPL集計方針を補強。

## ADR-011 | 2026-05-31 | 固定費はマスタ＋開始/終了年月で管理
- **決定**: 固定費（サブスク含む）は recurring マスタに「金額・開始年月・終了年月」を持つ。解約は終了年月のセットで表現（現運用の「0円グレーアウト」に相当）。
- **理由**: 過去履歴を保持したまま未来の自動計上を止められる（Q4）。
- **影響範囲**: recurring_rules テーブル。
- **状態**: 有効（Q4 確定）

## ADR-012 | 2026-05-31 | ウォレットは5種（みずほ/三井住友/三菱UFJ/楽天/PayPay残高）、引落口座は項目ごとに保持
- **決定**: 銀行4＋電子マネー(PayPay残高)1。固定費の引落口座は項目ごとに異なる（家賃・iDeCo=みずほ、カード=三井住友）ため、recurring_rules と card に引落ウォレットを持たせる。PayPay残高はプリペイド種別とし、1入力で残高自動反映（二重入力を解消）。
- **理由**: 実運用の口座構成と役割（給与=みずほ、カード引落=三井住友 等）に合わせるため。
- **影響範囲**: wallets(type=bank/prepaid), recurring_rules.settlement_wallet, cards.settlement_wallet。
- **状態**: 有効（Q3 確定）

## ADR-013 | 2026-05-31 | PL用語の確定（可処分所得＝トップライン／月次黒字＝最終ライン）
- **決定**: トップライン＝「可処分所得（手取り収入）」、最終ライン（余り）＝「月次黒字（貯蓄に回る額）」と表記する。最終ラインを"可処分所得"とは呼ばない。
- **理由**: 可処分所得＝収入−(税+社会保険料)＝手取り、という正式定義に合わせる。ユーザーの認識（トップライン＝可処分所得）が正しく、初期モックの「最終利益(可処分所得)」表記は誤りだった。
- **影響範囲**: ダッシュボードのラベル、ドキュメントの用語統一。
- **状態**: 有効

## ADR-014 | 2026-05-31 | 投資は元本移動を非計上・実現益のみ収入計上（実現主義）
- **決定**:
  - 株購入・iDeCo拠出など「元本の移動」はPLに載せない（excluded／口座→証券の振替）。日常PLには出さない。
  - 含み益は追わない（変動が大きく家計管理に不向き）。
  - 売却益・配当など「実現した利益」のみ、確定時に手入力で収入計上。専用カテゴリ「投資収益」(pl_type=income)で本業手取りと区別。
  - 証券・iDeCo残高（純資産）の管理は将来オプション。MVPスコープ外。
- **理由**: 実現主義は家計簿として保守的で正しい。ユーザーの「株益はたまに手入力でトップラインに混ぜる」案と一致。
- **影響範囲**: categories（投資収益）、transfers（証券への移動）、将来の資産管理機能。
- **状態**: 有効

## ADR-015 | 2026-05-31 | 現金は支出記録のみ・残高は管理対象外
- **決定**: 現金は決済手段として選択可（支出はPL/カテゴリに計上）。ただし現金ウォレットの残高は管理しない（追わない）。
- **理由**: 95%キャッシュレスで厳密な現金残高を把握しておらず、残高合わせの運用負荷を避けるため。
- **影響範囲**: wallets（現金は balance 非管理フラグ）、残高表示ロジック。
- **状態**: 有効

## ADR-016 | 2026-05-31 | 予実管理（予算 vs 実績）を中核機能に格上げ
- **決定**: 予実管理を主要機能の柱とする。年間予算を先に引き(12ヶ月先引き)、実績を月次で埋め、差異・達成率(%)を算出。収入/支出/収支の3軸、月次＋年間合計、複数FY横断比較に対応。済んだ月を視覚的に区別する（現運用の「黒塗り」）。
- **理由**: ユーザーのサマリータブの中核であり「絶対残したい」と明言（キャプチャ1）。当初docsの「予算消化率」は過小評価だった。
- **影響範囲**: budgets テーブル（収入/支出/収支の総額予算＋カテゴリ/テーマ別予算の2層）、ダッシュボード、新規「予実」ビュー、FY横断集計。
- **状態**: 有効（詳細はキャプチャ潰し込み中。下記要確認あり）

## ADR-017 | 2026-05-31 | FY開始月はユーザー設定で可変とする
- **決定**: 会計年度の開始月を固定（4月）にせず、ユーザー設定値とする。年度境界に依存する集計（FY別ロールアップ・予実）はこの設定を参照。
- **理由**: ユーザーが任意に設定したいと明言。実データもFY-1/2(5月始まり)とFY-3/4(4月始まり)が混在しており、可変が自然。
- **影響範囲**: users/settings に fiscal_year_start_month、全FY集計ロジック。ADR-007 を更新。
- **状態**: 有効

## ADR-018 | 2026-05-31 | 予算は総額（収入/支出/収支）のみ。カテゴリ別予算は持たない
- **決定**: 予実管理の予算は月次の総額（収入・支出。収支は差分で算出）レベルのみ。カテゴリ/テーマ別予算は当面持たない。実績側はカテゴリ集計で内訳を見られる。
- **理由**: ユーザー選択（総額のみ）。美容120万などの目標金額は"ビジョン層"として別概念で扱う。
- **影響範囲**: budgets テーブル（scope=income/expense, period=年月）。ADR-016 の粒度を確定。
- **状態**: 有効

## ADR-019 | 2026-05-31 | 日次入力は「1取引=1明細」、グリッドは集計ビュー
- **決定**: スプレッドシートの「日付×カテゴリのセル」はUI上の集計ビューとし、実体は transactions（取引1件1行）。
  - 各取引は金額に加え decision **決済手段(wallet)・メモ・発生日** を保持（シートには無い情報）。
  - 同日・同カテゴリに複数明細OK（セル値はその合計）。
  - `食費合計`・行合計・列合計（合計式）はすべて集計で算出（ADR-008と整合）。
  - 「特別管理用の列追加」は categories へのINSERTで対応（スキーマ変更不要）。
- **理由**: 決済手段をその場で記録することで残高に全連動（PayPay二重入力の解消＝ADR-012）。手入力アプリ化の中核価値。
- **影響範囲**: transactions, categories, 日次グリッドのビュー実装。
- **状態**: 有効（キャプチャ②で現設計の妥当性を確認）

## ADR-020 | 2026-05-31 | 月次は「予算(未確定) vs 実績(確定)」の予実グリッド
- **決定**: 月×セクション（固定費/変動費/収入）ごとに `確定/未確定` 状態を持つ。
  - 未確定 = 予算（手入力の概算）。確定 = 実績（取引の集計）。
  - 月が締まると確定化（現運用の「黒塗り」）。固定費・変動費・収入すべてが同一の予実グリッドに乗る。
  - 固定費の実額は月ごとに変動・途中解約・金額変更あり（確定月=実額、未確定月=予定額）。
- **理由**: 月次タブの見出しが ※確定/未確定 で運用されており、これが予実管理の実体（キャプチャ③④）。
- **影響範囲**: budgets/予実モデル、月次の確定フラグ、ダッシュボード予実ビュー、recurring_rules（予定額）と transactions（実額）の突合。
- **状態**: 有効。ADR-016 を具体化。

## ADR-021 | 2026-05-31 | 借入金は元金と利子を分離（利子のみPL費用）
- **決定**: 借入金の受取・元金返済は `excluded`（負債の増減、PL非計上）。**返済利子は支払い利息としてPL費用に計上**。
- **理由**: 支出タブに「支払い利息（①元金返済金額／②返済利子）」があり、利子は実際のコストだが元金は負債の減少のため。
- **影響範囲**: categories（支払い利息＝費用 / 借入・元金＝excluded）、ADR-010 を補強。
- **状態**: 有効

## ADR-022 | 2026-05-31 | 税金・社保は収入側(控除)で管理し支出に計上しない
- **決定**: 住民税・社会保険料等は payslip の控除内訳（pl_type=deduction）で管理。支出（固定費/変動費）には計上しない（二重計上防止）。手取り＝控除済みのため、PLトップライン（可処分所得）に織り込み済み。
- **理由**: 支出タブの「税金」セクションは収入タブ参照で¥0。控除は額面→手取りの過程であり費用ではない。
- **影響範囲**: PL集計ロジック、payslip/控除の扱い。
- **状態**: 有効

## ADR-023 | 2026-05-31 | クレカ明細は取引のビュー。締め日で請求サイクル自動判定
- **決定**:
  - クレカ取引の請求サイクルは「発生日＋カードの締め日」で自動判定（EPOS=28〜翌27、PayPay/Olive=1〜末）。現運用のEPOS月跨ぎ手動入れ替えを廃止。
  - 取引は1件のみ保持し、「日付×カテゴリ」ビュー（日次タブ相当）と「カード×請求サイクル」ビュー（カードタブ相当）の両方を集計で生成。日次↔カードの二重入力を解消。
  - カードの `種別` はカテゴリと同一体系（年会費・月額費=固定費、交際費・自己投資=変動費 等）。
  - 支払日の請求合計＝引落額を自動算出。消込は transfers（ADR-003）。
- **理由**: クレカタブが最も手運用が重い箇所（締め日違いの入れ替え・二重入力）。1入力・全連動の本丸。
- **影響範囲**: transactions, card_statements(closing/payment day), 各種ビュー。
- **状態**: 有効（2026-07-19注記: 実装は締め日情報を wallets に持ち請求サイクルを**都度算出**、消込は transfers の memo 締めキー方式。
  `card_statements` テーブル自体は現状未使用＝要確認リスト参照）

## ADR-024 | 2026-05-31 | プリペイドは全て残高管理。チャージは資金移動
- **決定**: ANA Pay / JAL Pay / PASMO / PayPay残高 等を type=prepaid のウォレットとして残高管理。
  - チャージ＝資金移動（transfer：資金元カード/口座 → プリペイド）。PL費用ではない。
  - 点活用の中継（JAL Pay→ANA Pay→PASMO）も振替の連鎖で表現。
  - 実際の消費はプリペイドからの支出として費用計上。これで各残高が実残高と一致。
- **理由**: ユーザーが全プリペイドの残高管理を選択（正確性重視）。
- **影響範囲**: wallets(type=prepaid), transfers, transactions。
- **状態**: 有効。🔶 管理対象プリペイドの最終リストはseed時に確定。

## ADR-025 | 2026-05-31 | ポイントは資産（残高管理）。取引は満額＋分割支払いを許容
- **決定**: ポイントを type=points のウォレット（台帳）として残高管理。
  - 獲得＝ポイント残高+（ポイント収入を計上）／使用＝ポイント残高−。取引は満額で記録。
  - 現運用の「値引き(−)＋ポイント収入(+)」両建ては廃止（二重計上の解消）。
  - **1取引が複数決済手段にまたがる「分割支払い（split tender）」**を許容する設計とする（例：カード¥8,000＋ポイント¥2,000）。取引1件に複数の支払い脚（payment legs）を持てる構造をPhase 3で設計。
- **理由**: ユーザーがポイントの資産管理を選択。経費精算/ポイントの両建て問題（ADR-010）と整合。
- **影響範囲**: wallets(type=points), transactions（支払い脚の分割）, 取引入力UI。
- **状態**: 有効。🔶 管理するポイント種別（EPOS/Vポイント/PayPay/マイル等）はseed時に確定。

## ADR-026 | 2026-05-31 | 残高台帳は廃止し、総資産・純資産を自動算出
- **決定**:
  - 全ウォレットの残高を取引・振替から自動算出（手動running-balance運用を廃止／ADR-002の具体化）。`口座残高合計`は集計で自動表示。
  - ウォレットを type（bank/prepaid/points/cash/credit_card）で分類し、各ウォレットに「資産合計に含めるか(include_in_assets)」フラグを持つ。
  - 「PayPayは銀行口座か」「PASMOは資産か」問題は型で解決：bankに分類せずとも prepaid として資産合計に含められる。
  - 台帳の「チャージ・入替／現金出し／移し替え」＝transfer、「給料」＝income、「経費」＝excluded として一本化。
  - 現金は概算（ADR-015）。投資資産は将来スコープ（ADR-014）。
  - **見せ方（確定）**: 総資産（資産系ウォレット合計）と純資産（総資産−カード未払い）の**両方を表示**。
  - **資産の範囲（確定）**: 銀行＋プリペイド＋ポイントを資産合計に含める（include_in_assets は既定ONで、必要なら個別OFF可）。
- **理由**: ユーザーの「全残高を常に正確に把握し総資産を見たい」思想は正しいが、スプレッドシートでは手更新でメンテ破綻していた。自動算出が解。
- **影響範囲**: wallets(type, include_in_assets), 残高/総資産/純資産の集計、資産ダッシュボード。
- **状態**: 有効

## ADR-027 | 2026-05-31 | 資産管理＝月末スナップショット自動生成＋資産形成の予実＋推移＋配当
- **決定**:
  - 月末残高スナップショットはシステムが自動生成（取引から算出。手入力不要。ADR-026）。
  - 資産形成の予実：月次「総資産」に対する目標と達成率(%)を表示（収支予実=ADR-016/020の資産版）。ビジョンの金額目標（資産1,000万〜3,000万）の具体化。
  - 総資産推移グラフ（資産種別の積み上げ）、配当収入の月次推移（年次比較）を資産ダッシュボードで提供。
  - 配当・分配金は投資収益（income / ADR-014）として計上。
  - ウォレットは想定より多い（銀行4＋プリペイド/ポイント多数＋ICOCA）。master-data §6 に反映。
  - **リコンサイル（確定）**: 任意で照合できる。普段は自動算出を信頼し、月末等に「実残高」を手入力したいときだけ照合し、ズレがあれば警告（記入漏れ検知）。→ wallets ごとに任意の balance_snapshot（実残高）を保持し、差分を表示。
  - **PayPayポイント(自動運用)（確定）**: 他のポイントと同様 points ウォレットとして残高管理。運用増減は残高更新で反映。
- **理由**: ユーザーの資産管理表（月末入力・目標達成率・推移・配当）を再現＋自動化するため。
- **影響範囲**: 残高スナップショット集計、資産予実(targets)、配当(income)、資産ダッシュボード、wallets、balance_snapshots（任意のリコンサイル）。
- **状態**: 有効（全確定）

## ADR-028 | 2026-06-06 | AWS構成は App Runner + RDS(PostgreSQL) + ECR/Secrets Manager
- **決定**: 本番は Next.js を `output:'standalone'` でコンテナ化し ECR→App Runner で稼働、DBは RDS for PostgreSQL（プライベート・TLS）。接続情報は Secrets Manager。手順は `docs/aws-deployment.md`。
- **理由**: ローカルと同じPostgreSQLで `schema.sql/seed.sql` を流用でき（ADR-005）、App RunnerはECS比で学習・運用が軽い。
- **影響範囲**: `Dockerfile`/`.dockerignore`/`next.config.ts`(standalone)、デプロイ手順、コスト（概算$20〜45/月）。
- **状態**: 有効（設計・成果物は準備完了。実プロビジョニングはユーザーのAWSアカウント・課金同意待ち＝実行ゲート）。

## ADR-029 | 2026-06-20 | 本番はサイト全体をBasic認証（単一PW）で保護。/inspect は残す
- **決定**: `middleware.ts` でサイト全体（全ページ＋API）をHTTP Basic認証で保護する。資格情報は環境変数 `BASIC_AUTH_USER` / `BASIC_AUTH_PASSWORD`。**両方が未設定のときは保護しない**（ローカル開発を妨げないため）。学習用の `/inspect`（DB全閲覧）は削除・本番無効化せず、この認証の内側で残す。
- **理由**: 本番（Vercel）に実家計データを入れる前提では、無認証公開＝全テーブル(銀行名・残高・メモ等)流出のリスク。一方ユーザーは `/inspect` でテーブルのデータ挙動を自分で観察したい明確な意図がある（学習目的）。削除ではなくアクセス制限で両立させる。単一ユーザーの現段階では「サイト全体を1パスワード」が最小コストかつ確実。
- **影響範囲**: `middleware.ts`/`proxy.ts`、Vercel環境変数。将来の本格的マルチユーザーログイン（ADR-004）導入時に置き換える。
- **状態**: 🔁 **一時保留（2026-06-20）**。Next 16 の `proxy`（旧middleware）方式で実装したところ、**Vercel の実行時に全ルートが500**（存在しないパスでも500＝ルーティング前段で失敗）になったため、本番復旧を優先し `proxy.ts` を一旦削除。env未設定で保護も効いていなかった。**再導入時は (1) Vercelのプレビュー環境で先に検証、(2) 安定した `middleware.ts` 方式または別手段を採用、(3) 実データ投入前に必ず適用**、の順で進める。決定（サイト全体を認証で守る方針）自体は有効。

## ADR-030 | 2026-06-20 | 固定費は予実突合で計上（recurring_rules=予定／transactions=実額、予定は自動INSERTしない）
- **決定**: 月次の固定費は `recurring_rules`（予定額）と当月の `fixed_cost` 取引（実額）を**突合して表示**する。**予定額を transactions へ自動INSERTはしない**（マスタ=予定、取引=実額の役割分担を保持）。
  - PLの「固定費」ライン＝各固定費項目 `COALESCE(実額, 予定額)` の合計。未確定（実額未入力）の月は予定額で埋まる＝現運用の「定額を12ヶ月仮置き」を再現。
  - アクティブ判定: `start_month ≤ 当月` かつ（`end_month IS NULL` または `end_month > 当月`）。解約は終了年月セットで翌月から自動除外（ADR-011）。
  - 突合キーは当面 `category_id`（seedでは固定費1項目=1カテゴリで成立）。将来 `transactions.recurring_rule_id` を足してより厳密化できる。
- **理由**: ADR-020（予算[未確定]vs実績[確定]の予実）の固定費版を最小実装するため。マスタを予定源にすることで毎月の手入力を排し、実額が異なる項目だけ後から修正する運用にできる。
- **影響範囲**: `lib/queries.ts`（`getFixedCostPlanVsActual`）、`app/page.tsx`（固定費の予実セクション・PL固定費ラインの算出）。ADR-020 を具体化。
- **状態**: 有効（まず表示まで。各行からの「実額で記録」操作は次段）。

## ADR-031 | 2026-07-05 | 検討終了物の撤去（AWS成果物・Docker・モックアップ・旧引き継ぎメモ）
- **決定**: リポジトリのノイズ削減のため、検討が終わり現運用で使っていない成果物を削除（すべてgit履歴に残る）。
  - **AWS移行の成果物**: `Dockerfile` / `.dockerignore` / `docs/aws-deployment.md`。本番は Vercel + Neon で稼働中のため AWS案（ADR-028）は不採用として撤去。ADR-028 はこれにより**更新済**（履歴として残す）。`next.config.ts` の `output:'standalone'` は Vercel でも無害なため現状維持（本番構成に触れない）。
  - **Dockerローカル環境**: `docker-compose.yml` / `docs/local-setup.md` / `db/run.ps1`。実際のローカル開発はポータブルPostgreSQL（C:\pgsql / trust認証）で確定しており（`.env.local` も `postgres@localhost`）、Docker経路は不使用。手順は `db/README.md` に集約。
  - **Phase 1 モックアップ**: `mockup/index.html` / ルート `index.html`（モックへのリダイレクト）。役目終了。
  - **旧引き継ぎメモ**: `docs/handoff.md`。2026-06-06時点のスナップショットで内容が陳腐化。進捗の単一の真実は `docs/roadmap.md`、DB起動手順は `db/README.md` へ移管。
  - **デモデータ**: `db/dev/demo_seed.sql`。本番で実データ運用中のため体感用デモは不要。`db/dev/queries.sql`（学習用クエリ集）は残す。
- **理由**: 実装に不要になった検討資料が実運用のドキュメントと混在し、どれが「正」か分かりにくくなっていたため。稼働中のアプリのコード・設定（app/ components/ lib/ db/schema・seed、next.config.ts）には一切手を入れない。
- **影響範囲**: docs/ ・ルート直下・db/ のファイル構成。`docs/development-process.md` と `db/README.md` の参照を更新。
- **状態**: 有効

## ADR-032 | 2026-07-05 | デザインを sumika の「Warm Kakeibo」テーマに刷新＋セッションログイン導入
- **決定**:
  1. **デザイン**: UIテーマを [sumika](https://github.com/horie-t/sumika)（家計簿SaaS・BSD 3-Clause License, Copyright (c) 2026, HORIE Tetsuya）の
     「Warm Kakeibo」テーマ（コーラル×クリームの温かいパステル・白カード・丸角・絵文字装飾・丸ゴシック）を基に全面刷新。
     `frontend/src/index.css` 由来のスタイルを `app/globals.css` に移植・改変し、Tailwind の @theme でパレット（slate/emerald/red等）を
     一括再マップして全ページに適用。フォントは Zen Maru Gothic（Hiragino Maru の意図をクロスプラットフォームで再現）。
     BSD-3 の条件に従い、著作権表記を `app/globals.css` 冒頭と本ADRに保持する。
  2. **認証**: ログイン/ログアウトを実装（ADR-029「サイト全体を認証で守る」の実現形）。sumika は Keycloak(OIDC) だが
     Vercel サーバーレスでは常駐サーバーが持てないため、**HMAC署名付きセッションCookie（httpOnly / 30日）** の軽量実装とした。
     - 資格情報・鍵は環境変数 `AUTH_USER` / `AUTH_PASSWORD` / `AUTH_SECRET`。**3つ全て設定されたときだけ有効**
       （未設定なら素通し＝ローカル開発と env 設定前の本番を止めない。ADR-029 と同じ安全設計）。
     - ガードは **middleware/proxy を使わない**（Next 16 + Vercel で全ルート500の前科・ADR-029）。
       各ページ先頭で `requireAuth()`（未ログイン→ /login へリダイレクト）、各APIハンドラ先頭で `requireAuthApi()`（401）を呼ぶ方式。
     - `/api/health` は死活監視用に無認証のまま。`/inspect` は既存の `INSPECT_KEY` ガードに加えログインも必要になる。
- **理由**: ユーザーがsumikaのデザインと「ログインがある」体験を高く評価し、機能は保ったまま見た目と認証を取り入れたいと要望。
  ライセンスがBSD-3で合法的に流用可能。認証はKeycloakの本格OIDCでなく、単一ユーザーの現段階に見合う最小実装を選択
  （将来のマルチユーザー化＝ADR-004 時に NextAuth 等へ置き換え可能）。
- **影響範囲**: `app/globals.css`（全面）、`app/layout.tsx`（フォント・セッション）、`components/TopNav.tsx`（sumika風ナビ＋ログアウト）、
  `lib/auth.ts`（新規）、`app/login/page.tsx`・`app/api/auth/login|logout`（新規）、全15ページ＋全14APIルートにガード追加、
  `app/page.tsx`（サマリーカード・配色）。
- **状態**: 有効（ADR-029 はこれにより**実現済みに更新**。本番有効化は Vercel の env 3つの設定が必要）

## ADR-033 | 2026-07-05 | デザインを独自の「Fresh Ledger」（warm green × 水色）に再構築
- **決定**: ADR-032 で移植した sumika のテーマから、配色・アイデンティティを独自に再構築。
  若葉グリーン（#4cb586系）を主役、水色（#4aafd5系）をサブアクセント（ナビのアクティブ・純資産・リンク）にした
  「Fresh Ledger」テーマとする。ブランド絵文字は 🌱、サマリーカードは 🌱💳💎。
  カード・ナビ等のCSS構造は引き続き sumika 由来（BSD-3の著作権表記は維持）だが、見た目の印象は別物にする。
- **理由**: ユーザーが「完全パクリに見えるのは癪。自分のデザインとして完成させたい。緑ベース＋水色も使いたい」と要望。
- **影響範囲**: `app/globals.css`（:root/@theme/グラデーション）、TopNav/ログインのブランド、ダッシュボードの配色。
- **状態**: 有効（ADR-032 のデザイン部分をこれで更新）

## ADR-034 | 2026-07-05 | カレンダー入力（/calendar）と取引の絞り込み
- **決定**:
  1. **カレンダー入力**: `/calendar` に1ヶ月のカレンダーを表示。各日セルにその日の支出合計（固定費＋変動費）を表示し、
     日付タップで「その日の入力フォーム（最初から開いた状態・発生日=その日）」と「その日の明細一覧」を開く。
     現運用スプレッドシートの「日次（支出）」タブの再現。選択状態はURL（`?m=&d=`）で持つサーバーレンダリング方式（クライアント状態なし）。
  2. **絞り込み**: `/transactions` に 種別/カテゴリ/決済手段 のフィルタ（GETフォーム）＋絞り込み結果の件数・合計表示。
     取引入力フォームのカテゴリにも部分一致の絞り込み入力を追加（49カテゴリから素早く選ぶ用）。
- **理由**: ユーザー要望（②③）。日次入力の主動線をスプレッドシートの操作感に寄せる。
- **影響範囲**: `app/calendar/page.tsx`（新規）、`lib/queries.ts`（getDailyTotals/getDayTransactions/TxFilter）、
  `app/transactions/page.tsx`、`components/AddTransactionForm.tsx`（defaultOpen・カテゴリ絞り込み）、TopNav。
- **状態**: 有効

## ADR-035 | 2026-07-05 | 固定費マスタに月額/年額区分（年額サブスク台帳）＋起動時オートマイグレーション
- **決定**:
  1. `recurring_rules` に `billing_cycle('monthly'|'yearly', 既定'monthly')` と `payment_month(1-12)` を追加。
     **年額サブスクは月次PL・固定費予実（ADR-030）に出さない**。支払った月に変動費（物品購入費等）として取引入力する
     現運用を維持し、`/fixed-costs` の「年額サブスク」セクションで年合計・月割換算・支払月を台帳管理する。
  2. **オートマイグレーション**: 冪等な `ADD COLUMN IF NOT EXISTS` を `lib/db.ts` の `ensureMigrated()` に置き、
     該当クエリの初回実行前に自動適用する。Neon（本番）の接続情報は sensitive env でCLIから取得できず、
     「コードのデプロイとDB変更の順序問題」を無くすため。破壊的変更はこの仕組みでは行わない。
     正式なDDLは `db/schema.sql`＋`db/migrations/` に残す（ローカルは適用済み）。
- **理由**: ユーザー要望（⑨）。年間払い（プライム等）が毎月の固定費に混ざると月次PLが実態とずれる。
- **影響範囲**: `db/schema.sql`・`db/migrations/001_billing_cycle.sql`・`lib/db.ts`・`lib/queries.ts`・
  `components/RecurringForm.tsx`・`app/fixed-costs/**`・`app/api/recurring/**`。
- **状態**: 有効

## ADR-036 | 2026-07-05 | 現運用スプレッドシートとの差分解消（比率/週次/年間予実/気分）
- **決定**: 現運用の「🔥(FY-3)人生設計シート」全23タブを精査し、アプリに無い運用のうち以下を採用して実装。
  1. **手取りに対する比率**: ダッシュボードの固定費・変動費行に「手取り比 X%」を表示（シート全域にある「収入(手取り)に対する比率」の再現）。
  2. **週次進捗**（`/weekly`）: 月曜始まりの週×変動費ルートグループの自動ロールアップ（直近12週）。「(週次)進捗」タブの再現。
  3. **年間予算達成**: `/year` に予算（targets）vs 実績の12ヶ月対比＋達成率を表示（サマリタブの「(FY)年間予算達成」）。
  4. **気分の記録**: `transactions.mood smallint(1-5)` を追加し、入力フォームに任意の絵文字ピッカー（😩😕😐🙂😆）。
     一覧・カレンダー明細に表示。Googleフォーム「いまどんな気持ちですか？」の再現（お金と感情の相関という現運用の思想を保持）。
- **不採用（Web化で不要と判断）**: 口座タブ5枚の手動転記・残高手計算（DB自動算出で代替済み）、日次シートの集計列（SQLで代替）、
  クレカタブの明細手動転記（取引入力に統合済み）、奨学金返済・旧クレカ（運用終了）、Googleフォーム入力チャネル（カレンダー入力で代替）。
- **保留（roadmapに記載）**: 暗号資産（bitFlyer ETH/BTC/XRP）の資産管理、5か年PL・将来月フォーキャスト、過去FY（FY-1/FY-2）データのインポート。
- **影響範囲**: `transactions.mood`（オートマイグレーション）、`lib/queries.ts`、`/weekly`（新規）、`/year`、入力フォーム、一覧表示。
- **状態**: 有効

## ADR-037 | 2026-07-05 | Googleのみログイン＋マルチユーザー対応（ADR-004の実現形）
- **決定**:
  1. **Googleログイン（OIDC認可コードフロー・自前実装）**: `GOOGLE_CLIENT_ID / GOOGLE_CLIENT_SECRET / AUTH_SECRET` が
     揃うと有効になり、ログイン画面は**「Googleでログイン」のみ**になる（パスワード入力は廃止・Google未設定時のみフォールバックで残存）。
     ライブラリ（Auth.js等）は使わず、`/api/auth/google`（認可リダイレクト＋state Cookie）と
     `/api/auth/google/callback`（コード→トークン交換・id_token検証・入場判定）の2ルート約150行で実装。
     依存ゼロで学習しやすく、Googleと直接TLS通信するため id_token の署名検証は aud/exp チェックで足りる。
  2. **入場判定（3段階）**: ①`AUTH_OWNER_EMAIL` と一致→**オーナー(user 1)** として入場（初回に users.email を自動で紐付け）
     ②users.email に登録済み→入場 ③`AUTH_ALLOWED_EMAILS`（カンマ区切り）に含まれる→**初期データつきでユーザー自動作成**
     （`lib/provision.ts`：汎用スターターのウォレット3種＋カテゴリ16種。オーナーの個人マスタはコピーしない） ④その他→拒否。
  3. **マルチユーザー化**: 全クエリ（lib/queries.ts）と全APIの `USER_ID=1` 固定を廃止し、
     セッション（`{id, email, name}` のHMAC署名Cookie）から `currentUserId()` で解決。認証無効時はオーナー(1)にフォールバック。
- **理由**: ユーザー要望（Googleのみログイン・他ユーザーへの提供）。パスワードを自前で預からないため漏洩リスクが構造的に消える。
  DBは当初からマルチユーザー設計（全テーブルにuser_id・ADR-004）だったため、変更はID解決の動的化のみで済んだ。
- **影響範囲**: `lib/auth.ts`（セッションv2）・`lib/provision.ts`（新規）・`app/api/auth/google/**`（新規）・
  `app/login/page.tsx`・`lib/queries.ts`・全APIルート。**必要env**: GOOGLE_CLIENT_ID / GOOGLE_CLIENT_SECRET / AUTH_SECRET /
  AUTH_OWNER_EMAIL（＋招待は AUTH_ALLOWED_EMAILS）。Google Cloud Console の承認済みリダイレクトURIに
  `https://<ドメイン>/api/auth/google/callback` を登録する。
- **状態**: 有効・**本番有効化済み**（Vercel/App Runner とも env 設定済み・`AUTH_ALLOWED_EMAILS` による招待制で運用中。2026-07-19確認。ADR-032 のパスワード認証はフォールバックに降格）

## ADR-038 | 2026-07-05 | AWS移行を再開（ADR-031の撤去判断を更新）
- **決定**: ユーザーの明示的な意向により AWS 移行（App Runner + RDS・ADR-028）を再開。
  ADR-031 で削除した `Dockerfile` / `.dockerignore` / `docs/aws-deployment.md` を git 履歴から復元し、
  手順書を現行の認証env（ADR-037）に合わせて更新。AWS CLI v2 を開発機に導入。
  **実プロビジョニングの実行ゲート**（ADR-028と同じ）: AWSアカウント＋`aws configure` 済み credentials・課金同意。
- **影響範囲**: Dockerfile / .dockerignore / docs/aws-deployment.md（復元・更新）。ADR-031 の該当部分を更新。
- **状態**: ✅ **稼働確認済み（2026-07-06）**。構成は当初案から2点変更:
  1. **ローカルDockerを使わない**（会社PCのDocker Desktopライセンス問題）→ CodeBuild でクラウドビルド。
  2. **VPCコネクタ不使用**（Google OAuth等の外向き通信にNAT +$32/月が必要になるため）→ App Runner デフォルトegress
     ＋RDSパブリックエンドポイント（TLS・強パスワード・SG開放は通常時DB停止で緩和）。
  日常は**デフォルト停止運用**（App Runner pause＋RDS stop＝ストレージ代のみ）。詳細・ハマりどころは docs/aws-deployment.md 6.4。

## ADR-039 | 2026-07-07 | 給与明細OCR（Gemini）＋サイトメタデータ整備
- **決定**:
  1. **給与明細OCR**: 給与明細の編集画面に「📷 画像から自動入力」を追加。画像をクライアント側で縮小（最大1600px JPEG）→
     `/api/payslips/ocr` → **Gemini API**（`GEMINI_API_KEY`・Google AI Studio無料キー、モデルは `GEMINI_MODEL` で変更可・既定 gemini-2.5-flash）で
     支給/控除の内訳・労働時間をJSON抽出→フォームに流し込む。**キー未設定の間はボタン非表示＆API 400**（認証と同じ「envで有効化」方式）。
     画像はDBに保存しない（読み取り結果のみ利用）。読み取り結果は必ず人間が確認してから保存する運用。
  2. **メタデータ**: favicon（🌱ブランドタイルのSVG・`app/icon.svg`）、OGP、タイトルテンプレートを整備。
     個人の家計アプリのため **robots は noindex**。
- **理由**: 現運用の「給与明細OCR＋手入力」（master-data記載）の再現。roadmapのP3項目。
- **影響範囲**: `app/api/payslips/ocr/route.ts`（新規）・`components/PayslipForm.tsx`・`app/payslips/[period]/edit/page.tsx`・
  `app/icon.svg`（新規）・`app/layout.tsx`。**有効化に必要なenv**: `GEMINI_API_KEY`（Vercel/App Runnerに設定）。
- **状態**: 有効（コード完成・キー設定待ち）

## ADR-040 | 2026-07-19 | 取引の冪等キー（client_key）で二重入力を防止
- **決定**: `transactions.client_key text` を追加し、部分ユニーク索引
  `uq_tx_user_client_key(user_id, client_key) WHERE client_key IS NOT NULL` を張る。
  - 入力フォームは1回の入力につき `crypto.randomUUID()` を1つ生成して送信（保存成功で作り直し。編集時は送らない）。
  - APIは同一 client_key の既存取引があれば**新規作成せず既存IDを返す**（`duplicate:true`）。
    同時実行でユニーク違反(23505)になった場合も同様に既存を返す＝3層（クライアント/API/DB）で冪等。
  - DDLはオートマイグレーション（ADR-035）＋正式控え `db/migrations/003_client_key.sql`・`db/schema.sql`。
- **理由**: 保存ボタン連打・通信リトライ・戻る→再送信での二重計上を、UIのdisabledだけに頼らずDBレベルで確実に防ぐため。
- **影響範囲**: `lib/db.ts`・`db/schema.sql`・`db/migrations/003_client_key.sql`・
  `app/api/transactions/route.ts`・`components/AddTransactionForm.tsx`。
- **状態**: 有効。※コミット 2ec4c2f のメッセージは「ADR-039」表記だが、並行作業のOCR（ADR-039）と番号が衝突したため本エントリで **ADR-040 に繰り下げ**。

## ADR-041 | 2026-07-19 | 分析ページ（/analytics）＝月次比較とビジュアル化
- **決定**: `/analytics`（ナビ「分析🧮」）を新設。
  ①今月 vs 直近平均（収入/支出/黒字の3カード。差額と%、データのある過去月のみで平均）
  ②直近12ヶ月の収入・支出（棒）＋黒字（折れ線）の複合チャート
  ③支出カテゴリ今月トップ8の前月比。
  チャートはライブラリ非依存のインラインSVG（`/assets`・`/year` と同方式）で「Fresh Ledger」配色（ADR-033）に統一。
- **理由**: ユーザー要望「他の月との比較・ビジュアル的な表現」。依存追加ゼロで軽量・テーマ一貫・学習しやすい。
- **影響範囲**: `app/analytics/page.tsx`（新規）・`lib/queries.ts`（getMonthlySeries/getCategoryMoM）・`components/TopNav.tsx`。
- **状態**: 有効。※コミット 4578da9 のメッセージは「ADR-040」表記（上記と同じ番号衝突。本エントリで **ADR-041 に繰り下げ**）。

## ADR-042 | 2026-07-19 | 通知基盤（変動費しきい値のメール通知）＋設定画面（/settings）
- **決定**:
  1. **ルールのテーブル化**: `notification_rules`（user_id・kind・threshold・channel・enabled）。
     既定ルールは変動費しきい値 **10/15/20/25/30万円** の5行（seed・provision・オートマイグレーションのフラグ方式
     `users.notif_defaults_seeded` で一度だけ投入＝全削除しても勝手に復活しない）。
     **カスタマイズ＝行の追加/編集**でコード変更不要。kind は将来増やせる（例: fixed_cost_threshold）。
  2. **「月1回だけ」の担保**: `notification_log` の UNIQUE(rule_id, period) が実体。送信前にログ行を先に確保
     （ON CONFLICT DO NOTHING）することで同時リクエストの多重送信も防ぎ、送信失敗時はログを取り消して次回再挑戦。
  3. **トリガーは取引の書き込み時**（POST/PUT /api/transactions 後）: cron不要でサーバーレスと相性が良い。
     当月（JST）の変動費合計を集計→「有効・当月未通知・到達済み」のルールへメール。過去月の入力/修正では発火しない
     （バックフィル時の通知スパム防止）。通知の失敗は取引の保存を失敗させない。
  4. **送信は Resend の HTTP API**（`lib/notify.ts`・依存パッケージゼロ）。`RESEND_API_KEY` 未設定の間は静かにスキップ
     （OCR/認証と同じ「envで有効化」方式）。宛先は users.email（＝Googleログインのアドレス）。
     差出人は既定 `onboarding@resend.dev`、独自ドメイン検証後は `NOTIFY_EMAIL_FROM` で変更可。
  5. **設定画面 `/settings`（⚙️）**: 通知ルールの追加/ON・OFF/削除・送信履歴の閲覧に加え、
     **FY開始月の変更UI**（ADR-017の残タスク）も同居させた。
- **理由**: ユーザー要望「変動費が10〜30万円のタイミングでログインメールへ通知＋今後カスタマイズできる形で」。
  ルール駆動＋書き込み時判定＋envゲートは、既存アーキテクチャ（サーバーレス・依存最小・env有効化）と一貫する。
- **影響範囲**: `db/schema.sql`（notification_rules / notification_log / users.notif_defaults_seeded）・
  `db/migrations/004_notifications.sql`・`lib/db.ts`・`lib/notify.ts`（新規）・`lib/queries.ts`・`lib/provision.ts`・
  `app/api/transactions/**`（フック）・`app/api/notification-rules/**`・`app/api/settings/route.ts`・
  `app/settings/page.tsx`・`components/FySettingForm.tsx`・`components/NotificationRulesPanel.tsx`・TopNav。
  **有効化に必要なenv**: `RESEND_API_KEY`（任意で `NOTIFY_EMAIL_FROM`）。
- **状態**: 有効（コード完成・ローカルでルールCRUD/判定SQL/多重送信ガード検証済み。メール送信はキー設定待ち）

## ADR-043 | 2026-07-19 | 暗号資産の資産管理（評価額の手入力・価格API非依存）
- **決定**:
  1. `wallets.type` に **'crypto'** を追加。銘柄（bitFlyer ETH/BTC/XRP等）＝1ウォレット。
  2. **crypto の残高＝`balance_snapshots` の最新 `actual_balance`（評価額の手入力）を「正」とする**。
     取引集計ベースの残高算出（ADR-002）の唯一の例外。理由：暗号資産は取引が無くても評価額が変動するため、
     集計では価値を表現できない。既存のリコンサイル基盤（ADR-027）をそのまま評価額の記録に転用。
  3. 価格APIには依存しない（ユーザー選択）。購入=振替（銀行→銘柄・PL非計上）、売却益=投資収益（実現主義・ADR-014と整合）。
  4. `/assets` に「₿ 暗号資産」パネル：銘柄の追加（`POST /api/wallets`・当面crypto専用）と評価額のインライン更新
     （既存 `POST /api/snapshots` を転用）。最新評価額は総資産・純資産・月次推移（各月末時点の最新スナップショット）に自動反映。
  5. ついでに `POST /api/snapshots` の2つの穴を修正：他ユーザーの wallet_id に書けた所有者チェック漏れ、
     bigint（pgでは文字列）の wallet_id が数値検証で弾かれるバグ。
- **影響範囲**: `db/schema.sql`・`db/migrations/005_crypto.sql`・`lib/db.ts`・`lib/queries.ts`
  （getWalletBalances/getAssets/getAssetBreakdown/getAssetTrend にcrypto例外、getCryptoWallets新規）・
  `app/api/wallets/route.ts`（新規）・`app/api/snapshots/route.ts`・`components/CryptoPanel.tsx`（新規）・
  `app/assets/page.tsx`・種別ラベル各所。
- **状態**: 有効（ローカルで銘柄追加→評価額入力→総資産/内訳/推移への反映を検証済み）

## ADR-044 | 2026-07-19 | 5か年PL・将来月フォーキャスト（/forecast）
- **決定**: `/forecast`（ナビ「5か年🔮」）を新設。当FY開始月から**60ヶ月**を、
  - **実績月**（先月以前）＝取引の実額
  - **未来月・今月**＝`targets`（/budget の月次目標）を優先、無い月は
    **固定費＝月額マスタのアクティブ判定合計（ADR-030と同じ）＋変動費・収入＝直近6ヶ月平均**で自動見込み
  として構成。FYサマリカード（5年分の黒字・期末想定純資産）、純資産の60ヶ月予測ライン（現在の純資産起点に黒字を累積）、
  FY別の月次テーブル（実績/今月(見込)/目標/見込み の区分表示）を表示。**新テーブルは作らない**
  （見込みの手入力は既存の targets＝予実画面がそのまま入力口になる）。進行中の今月は実績が途中のため見込み扱い（FY合計の過小評価防止）。
- **理由**: 現運用シートの「未来月に定額を仮置きして年間・複数年を見通す」運用の再現（ADR-036で保留にしていた項目）。
  マスタと実績から見込みを自動生成することで、手で60ヶ月埋める運用を排した。
- **影響範囲**: `app/forecast/page.tsx`（新規）・`lib/queries.ts`（getForecastInputs）・TopNav。
- **状態**: 有効

## ADR-045 | 2026-07-20 | 現運用スプレッドシート（FY-1〜FY-3）の過去データ一括インポート
- **決定**: 「🔥(FY-3)人生設計シート」xlsx から過去実績を一括取込する仕組みを構築（`db/import/gen_import.py` → SQL生成 → psql/Neon SQL Editorで実行）。
  1. **冪等設計**: 全取引に決定的な `client_key`（`imp:種別:期間:項目`）を付与し `ON CONFLICT DO NOTHING`。**何度流しても二重登録されない**（ADR-040の冪等キー基盤を転用）。
  2. **残高を壊さない**: 過去取引は**支払い脚（transaction_legs）なし**で投入。PLは transactions のみで集計される設計（ADR-002/019）のため損益は正しく、現在のウォレット残高計算には一切影響しない。
  3. **取込内容**: 日次変動費（FY-2/FY-3日次タブ・セル単位）／月次固定費・返済利子・元金返済（FY-1〜3、元金はexcluded=ADR-021）／収入5種（経費精算・借入金はexcluded=ADR-010）／給与明細25ヶ月（FY-3は支給内訳・労働時間付き、FY-1/2は総支給＋控除内訳）／月末残高スナップショット157件（銀行・プリペイド・ポイント）／FY-3未確定月の収支目標→targets（/forecastの見込みに直結）。
  4. **シート内不整合の裁定**: FY-1/FY-2境界月（2025-04）は両タブに存在→**FY-2を正**。合成行（①+②等）は二重計上防止でスキップ。月次タブ直入力（引越し費等）は**月次を正**とし差分を月末調整取引で補完。日次＞月次の月（2件・計6,700円）は**日次を正**とし警告記録。
  5. **スコープ外**: 証券・iDeCo・サンリオ株の残高（ADR-014で将来オプションと決定済みのため見送り。将来 type='securities' 拡張で対応可）。口座タブ・クレカタブ・サマリ・週次（アプリ側で自動集計に代替済み＝ADR-036）。
  6. **カットオフ**: ~~2026-07-01~~ → **全期間取込に更新（2026-07-20）**。ユーザー確認の結果、本番アプリに実データは未投入だったため
     重複リスクなし。当面は**アプリとスプレッドシートの並走運用（シートが正）**とし、シート更新後に再生成→再実行で差分だけ追加する
     （冪等キーがあるため何度でも安全）。
  7. **個人データの扱い**: 生成SQL（実金額入り）は `data/`（gitignore済み）に置き、**リポジトリにはコミットしない**。ジェネレータ（コードのみ）は `db/import/` にコミット。
- **検証**: ローカルで全確定月×全区分（固定費/変動費/収入）をシートの月次合計と突合し**NG 0件**。再実行で件数不変（冪等）を確認。取引2,211件・給与明細25・スナップショット157・目標18。
- **影響範囲**: `db/import/gen_import.py`・`db/import/validate.py`（新規・コードのみ）・`data/import-sheet-fy1-fy3.sql`（git外）・categories に取込用6件追加（脱毛(プラム)/ChatGPTプラス/しがく会費/Amazon Prime/テレ東BIZ/(取込)食費一括※入力不可）。
- **状態**: ローカル取込完了（全期間・取引2,301件）・**本番は Neon SQL Editor での実行待ち**（`data/import-sheet-fy1-fy3.sql`）

## ADR-046 | 2026-07-20 | 機能の表示ON/OFF（設定画面からナビをカスタマイズ）
- **決定**: ユーザーごとに「使わないページ」をナビゲーションから隠せるようにする。
  1. `users.hidden_pages jsonb`（非表示にしたページのhref配列）を追加。マルチユーザー（ADR-037）前提でユーザー単位の設定。
  2. ナビ定義を `lib/nav.ts` に一元化（TopNavと設定画面で共有）。「概要」「設定」は `always=true` で非表示不可。
  3. `/settings` の最上部に「🧩 機能の表示」パネル：タップでON/OFF→保存（`PUT /api/settings` の部分更新に統合）。
     APIは許可リスト（HIDEABLE_HREFS）で検証し不正なhrefを拒否。
  4. **隠すのはナビ表示のみ**（URL直打ちは可・データ無影響）。閲覧中のページは設定に関わらずナビに出す（迷子防止）。
- **理由**: ユーザー要望「人によって使う項目と使わない項目があるので設定で分けたい」。招待ユーザー（ADR-037）にも各自の設定が効く。
- **影響範囲**: `lib/nav.ts`（新規）・`components/TopNav.tsx`（NAV定義を移設・hidden対応）・`components/FeatureTogglePanel.tsx`（新規）・
  `app/settings/page.tsx`・`app/api/settings/route.ts`（部分更新化）・`app/layout.tsx`（hidden_pages取得）・`lib/queries.ts`・
  `db/schema.sql`・`db/migrations/006_hidden_pages.sql`・オートマイグレーション。
- **状態**: 有効（ローカルでOFF→ナビ消滅・不正href拒否・復元まで検証済み）

## 要確認リスト（クローズ済み・2026-07-19棚卸し）
- ビジョン/目標レイヤー → ✅ `/vision`（自由記述の箱・vision_notes）として実装済み。予実/資産目標との数値連動は将来検討（roadmap）。
- `食費合計`（日次）＝`食費(1人)`（月次）の同一性 → ✅ seedで同一カテゴリ体系に統合済み。
- ADR-024/025 の🔶（管理するプリペイド/ポイントの最終リスト） → ✅ seedで確定（プリペイド7種：PayPay残高/ANA Pay/JAL Pay/PASMO/ICOCA/VポイントPay/メルペイ、ポイント3種：Ponta/d/PayPayポイント(自動運用)）。
- ＜確定済＞ Q1=excluded / Q2=KPI管理 / Q3=ウォレット5種 / Q4=マスタ+終了年月 / Q5=自動集計
  / カード支払日=翌月 / 交際費=食費グループ / 用語(ADR-013) / 投資(ADR-014) / 現金(ADR-015)

## 要確認リスト（オープン・2026-07-19棚卸し）
- ~~card_statements テーブルが未使用~~ → ✅ クローズ（2026-07-19 決定：**撤去せず温存**。現方式（都度算出＋memo締めキー）で運用上の問題がなく、
  DROPは破壊的変更でリスクのみ。将来厳密消込が必要になったら使う）。
- ~~FY開始月の設定UI が無い~~ → ✅ クローズ（2026-07-19 `/settings` に実装・ADR-042）
- ~~本番(Vercel)の認証有効化の確認~~ → ✅ クローズ（2026-07-19 ユーザー確認：Vercel も Google ログイン有効化済み・`AUTH_ALLOWED_EMAILS` で招待制運用中）
- **OCRの有効化**: コード完成（ADR-039）。`GEMINI_API_KEY` 設定待ち（設定までボタン非表示＝安全側）。
